{
  "name": "gun-utils",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cleanDist": "lerna run clean --stream",
    "clean": "lerna clean && rm -rf node_modules",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "dependencies": {
    "@jest/globals": "^29.3.1",
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.3"
  }
}
